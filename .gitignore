# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
supabase/.temp/

# Environment variables
.env
.env.local
.env.*.local

# Eval harness run artifacts — regenerated by `gate.ts` on every run.
# CI uploads these as artifacts (see .github/workflows/eval-harness.yml);
# there's no reason to track the latest-run output in git.
tools/eval-harness/reports/
# Agent-local config directory (Claude Code). Per-developer state.
.claude/
